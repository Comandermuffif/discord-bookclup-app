version: "2.0"
services:
  postgres:
    image: postgres:16.1
    environment:
      POSTGRES_PASSWORD: test
    ports:
      - 5432:5432
    volumes:
      - pgdata:/var/lib/postgresql/data/pgdata
volumes:
  pgdata: {}